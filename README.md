# Docker image for [taskotron/resultsdb](https://pagure.io/taskotron/resultsdb)

initialize your database:
```
docker-compose run resultsdb python run_cli.py init_db
```

Run
```
docker-compose up -d
```
